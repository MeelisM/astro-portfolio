---
import skillsData from "../content/skills.json";

interface Skill {
  name: string;
  icon: string;
}

interface SkillsData {
  skills: Skill[];
}

interface Props {
  filter?: string[];
  bgColor?: string;
  bgHover?: string;
  textHover?: string;
  textColor?: string;
}

const { skills }: SkillsData = skillsData;

const {
  filter = [],
  bgColor = "bg-primary-badge",
  bgHover = "hover:bg-secondary-badge",
  textHover = "hover:text-primary-badge",
  textColor = "text-secondary-badge",
}: Props = Astro.props;

const filteredSkills =
  Array.isArray(filter) && filter.length > 0
    ? skills.filter((skill) => filter.includes(skill.name))
    : skills;
---

<ul class="flex flex-wrap gap-1">
  {
    filteredSkills.map((skill) => (
      <li
        class={`inline-flex items-center px-2 py-1 rounded-[12px] ${bgColor} ${textColor} ${bgHover} ${textHover} text-primary`}
        aria-label={skill.name}
      >
        <span class="mr-1 w-4 h-4" set:html={skill.icon} aria-hidden="true" />
        <span class="text-sm">{skill.name}</span>
      </li>
    ))
  }
</ul>

<style>
  span :global(svg) {
    fill: currentColor;
  }
</style>
